import { styled } from '@/styles';

const Layout = styled('div', {
  maxWidth: 980,
  margin: '0 auto',
  display: 'flex',
  flexDirection: 'column',
  gap: 4,
  px: 1,
  pt: 3,
  pb: 2,
});

export default Layout;
