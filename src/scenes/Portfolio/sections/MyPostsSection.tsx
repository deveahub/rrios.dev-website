import Section, { SectionTitle } from '@/scenes/Portfolio/components/Section';

const MyPostsSection = () => (
  <Section>
    <SectionTitle>My posts</SectionTitle>
  </Section>
);

export default MyPostsSection;
