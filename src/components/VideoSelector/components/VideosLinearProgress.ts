import LinearProgress from '@/components/LinearProgress';
import { styled } from '@/styles';

const VideosLinearProgress = styled(LinearProgress, {
  position: 'fixed',
  bottom: 0,
  left: 0,
});

export default VideosLinearProgress;
